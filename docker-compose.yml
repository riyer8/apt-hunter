services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: aptwatch
      POSTGRES_PASSWORD: aptwatch
      POSTGRES_DB: aptwatch
    ports:
      - "5432:5432"
    volumes:
      - aptwatch_pg:/var/lib/postgresql/data

volumes:
  aptwatch_pg:
